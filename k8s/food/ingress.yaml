---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: food-ingress
  namespace: next-orders
  labels:
    app: food
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - food-demo.nextorders.ru
      secretName: food-demo.nextorders.ru
  rules:
    - host: food-demo.nextorders.ru
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: food-service
                port:
                  number: 3000
